namespace Day10;

public static class Solver
{
    public static (int, int) Solve(string data)
    {
        var lines = data.Split("\n").ToList();

        var part1 = 0;
        var part2 = 0;
        
        //TODO
        
        return (part1, part2);
    }
}